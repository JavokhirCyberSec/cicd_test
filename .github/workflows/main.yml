name: Demo Workflow
on: workflow_dispatch
jobs: 
  print_demo:
   runs-on: ubuntu-latest
   steps: 
    - name: Print to console
      run: echo Hello GitHub Actions
    - name: Print a few line of code
      run: |
        echo First line!
        echo Second line!

 
